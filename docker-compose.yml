version: "3.8"

services:
  streamlit:
    build: .
    ports:
      - "8502:8501"
    volumes:
      - ./app.py:/app/app.py
      - ./data:/app/data
    environment:
      - STREAMLIT_SERVER_PORT=8501
      - STREAMLIT_SERVER_ADDRESS=0.0.0.0
    restart: unless-stopped
